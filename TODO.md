# TODO: Adopt Soft UI Dashboard Laravel Style with Login & User Management

## Steps (Approved Plan Breakdown):

1. **[x]** Install Laravel Breeze for auth scaffolding (login/register/dashboard).

2. **[ ]** Integrate Soft UI Design System assets (CSS/JS via CDN/npm).
3. **[ ]** Build guest landing page (update welcome.blade.php) with Soft UI style and login link.
4. **[ ]** Style login/register pages with Soft UI (gradient bg, forms).
5. **[ ]** Create admin dashboard with Soft UI components (navbar, cards, charts).
6. **[ ]** Add User Management: UserController, admin/users routes/views (CRUD table).
7. **[ ]** Update global layout (app.blade.php) with Soft UI navbar/sidebar.
8. **[ ]** Run `composer install`, `php artisan breeze:install`, `npm install && npm run build`, migrate.
9. **[ ]** Test: serve app, login, access admin/users.

**Current Progress: Starting step 3 (guest page first as specified).**

